<?php
class dtms extends AppModel
{
    public static function login($username, $password)
    {
       /* $db = DB::conn();

       $userExists = $db->row('SELECT * FROM user WHERE username = ? AND password = ?', array($username, $password));*/

    }
}